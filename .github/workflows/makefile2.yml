name: Makefile CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install dependencies
      run: ./do_release.sh
      run: tar -cf archive.tar output/*

    - uses: actions/upload-artifacts@v3
    - name: Run check
      with:
       name: archive
       path: archive.tar
